import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
    <App />,//component : HTML을 반환하는 함수 , 자바스크립트와 HTML의 조합을 JSX라고 부름
  document.getElementById('root')
);
